apiVersion: apps/v1
kind: Deployment
metadata:
  name: django-node2-deploy
spec:
  replicas: 1
  selector:
    matchLabels:
      app: django
      node: node2
  template:
    metadata:
      labels:
        app: django
        node: node2
    spec:
      nodeName: kube-node2
      imagePullSecrets:
        - name: dockerhub-cred
      containers:
        - name: django
          image: jjunch/django:15
          imagePullPolicy: Always
          env:
            - name: DJANGO_DB_ENGINE
              value: django.db.backends.postgresql
            - name: DJANGO_DB_NAME
              value: mysite
            - name: DJANGO_DB_USER
              value: mysite
            - name: DJANGO_DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgres-secret
                  key: db-password
            - name: DJANGO_DB_HOST
              value: postgres-svc
            - name: DJANGO_DB_PORT
              value: "5432"
          ports:
            - containerPort: 8000
